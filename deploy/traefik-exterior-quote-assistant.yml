http:
  routers:
    exterior-quote-assistant:
      rule: "Host(`sentinelforge.tech`) || Host(`www.sentinelforge.tech`) || Host(`quote.sentinelforge.tech`) || Host(`srv1674962.hstgr.cloud`)"
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: exterior-quote-assistant

  services:
    exterior-quote-assistant:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:8501"
